import { SkillDisplayPipe } from './skill-display.pipe';

describe('SkillDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
